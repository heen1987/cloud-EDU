# 5강: K8S 클러스터 설치

> 원본: `5.2026년 새싹(SeSAC) 클라우드 네이티브 교육 자료(K8S 클러스터 설치).pdf` (45p)
> 대응 실습 결과물: `../01_실습자료/k8s_cluster_vars/`(원본 `cp-cluster-vars.zip`)
> 출처: K-PaaS Github "컨테이너플랫폼 클러스터 설치가이드" (`https://github.com/K-PaaS/cp-deployment`)

이 강의는 K8s 이론보다 **K-PaaS 컨테이너 플랫폼의 실제 설치 절차**에 초점을 둡니다.

## 목차
1. **Container Platform 설치개요**
2. **클러스터설치준비** — 배포흐름, 시스템구성, 설치조건(Prerequisite)
3. **클러스터설치** — 7단계 설치 절차, 클러스터 확인, 삭제

---

## 핵심 개념 정리

### 설치 파이프라인
```
① 인스턴스 환경구성 → ② 클러스터 설치(Kubespray) → ③ 포털 설치 → ④ 필요 서비스 설치
```
**CP 클러스터 배포흐름**: IaaS(리소스 생성) → Kubespray로 Kubernetes Cluster 구성 → 클러스터 정보 변경 → Container Platform 패키지 배포

### 노드 구성 요소
| 노드 | 권장 개수 |
|---|---|
| Control Plane(Master) | 테스트 1개 / 운영 3개 이상 |
| Worker | NFS 사용 시 1개 이상, Rook-Ceph 사용 시 3개 이상 |
| Storage(NFS 서버) | NFS 스토리지 사용 시 필요 |
| Load Balancer | Control Plane 2개 이상(HA)일 때 필요 |
| ETCD | Control Plane 2개 이상일 때 external/stacked 선택 |

### 방화벽 필수 포트
| 노드 | 포트 |
|---|---|
| Control Plane | etcd(2379-2380), API Server(6443), Kubelet API(10250), scheduler(10251), controller-manager(10252), Read-Only Kubelet(10255), Calico VXLAN(UDP 4789), NFS(111/2049) |
| Worker | Kubelet API(10250), Read-Only Kubelet(10255), NodePort(30000-32767), Calico VXLAN(UDP 4789), NFS(111/2049) |

### 주요 소프트웨어 버전
Kubespray 2.23.0, Kubernetes 1.27.5, CRI-O 1.27.1, MetalLB 0.13.9, Ingress Nginx 1.8.2, Helm 3.12.3, Istio 1.19.0, Podman 3.4.4, OpenTofu 1.6.0, nfs-provisioner 4.0.2, Rook Ceph 1.12.3, Kubeflow 1.7.0

### 설치 사양 요건
Ubuntu 22.04, RAM 16G 이상(최소 8G), CPU 4개 이상(최소 2개), 모든 노드 간 완전한 네트워크 연결 필요

---

## 클러스터 설치 절차 (7단계)

```
환경구성 → NFS 서버설치 → SSH key 생성/배포 → Deployment 다운로드 → 환경변수 입력 → 쉘스크립트 배포 → 설치확인
```

### 1. 인스턴스 환경구성
Control Plane 1 + Worker 3 + NFS 스토리지 1 (전 노드 Ubuntu 22.04)

### 2. NFS 스토리지 설치 (NFS 서버 인스턴스에서)
```bash
ssh -i {ssh접속키} ubuntu@{nfs인스턴스ip}
sudo apt-get update
sudo apt-get install -y nfs-common nfs-kernel-server portmap
sudo mkdir -p /home/share/nfs
sudo chmod 777 /home/share/nfs
sudo vi /etc/exports
# Control Plane/Worker 전체 노드 Private IP를 아래 형태로 등록
# /home/share/nfs {IP}(rw,no_root_squash,async)
sudo /etc/init.d/nfs-kernel-server restart
sudo systemctl restart portmap
sudo exportfs -v   # 등록한 노드가 모두 출력되면 정상
```

### 3. SSH Key 생성 및 배포
- 클러스터 자동설치(Kubespray)를 위해 별도 `id_rsa`/`id_rsa.pub` 키페어를 새로 생성, 모든 노드의 `authorized_keys`에 등록

```bash
# Control Plane 노드에서
ssh-keygen -t rsa -m PEM -N '' -f $HOME/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub          # 공개키 복사
vi .ssh/authorized_keys        # 마지막 줄에 붙여넣기
# Worker 노드 3개에도 동일 반복
```

### 4. Deployment 다운로드 (Control Plane 노드에서만)
```bash
git clone https://github.com/K-PaaS/cp-deployment.git -b branch_v1.7.x
```

### 5. 환경변수 정의
```bash
cd cp-deployment/single/
vi cp-cluster-vars.sh
```
주요 입력 항목:
- Control Plane: `KUBE_CONTROL_HOSTS`, `MASTER{n}_NODE_HOSTNAME/USER/PRIVATE_IP/PUBLIC_IP`
- LoadBalancer: `LOADBALANCER_DOMAIN` (Control Plane 2개 이상 시 필수)
- ETCD: `ETCD_TYPE`(external/stacked), `ETCD{n}_NODE_HOSTNAME/PRIVATE_IP`
- Worker: `KUBE_WORKER_HOSTS`, `WORKER{n}_NODE_HOSTNAME/PRIVATE_IP`
- Storage: `STORAGE_TYPE`(nfs/rook-ceph), `NFS_SERVER_PRIVATE_IP`
- MetalLB: `METALLB_IP_RANGE`, `INGRESS_NGINX_IP`
- Kyverno: `INSTALL_KYVERNO`(Y/N)
- CSP LoadBalancer Controller: `CSP_TYPE`(NHN/NAVER) 및 인증정보

> 이 프로젝트 폴더의 `01_실습자료/k8s_cluster_vars/cp-cluster-vars.sh`가 이 실습에서 실제로 작성한 환경변수 파일입니다.

### 6. 쉘스크립트 배포
```bash
./deploy-cp-cluster.sh
```
필요 패키지 설치 → 환경변수 설정 → Ansible playbook으로 클러스터 설치 진행

### 7. 클러스터 설치확인
```bash
kubectl get nodes
kubectl get pods
kubectl get pods -n kube-system
```

### (부가) 클러스터 삭제
```bash
./reset-cp-cluster.sh
```

---

## 주요 명령어 목록

| 명령어 | 설명 |
|---|---|
| `ssh -i {키} ubuntu@{IP}` | 원격 인스턴스 SSH 접속 |
| `sudo exportfs -v` | NFS 공유 설정·연결 노드 확인 |
| `ssh-keygen -t rsa -m PEM -N '' -f $HOME/.ssh/id_rsa` | RSA 키페어 생성 |
| `git clone ... -b branch_v1.7.x` | 클러스터 설치 소스 다운로드 |
| `./deploy-cp-cluster.sh` | 클러스터 설치 실행 |
| `kubectl get nodes` | 노드 상태 확인 |
| `kubectl get pods [-n kube-system]` | Pod 상태 확인 |
| `./reset-cp-cluster.sh` | 클러스터 삭제 |

## 중요 용어集

| 용어 | 설명 |
|---|---|
| ETCD | 클러스터 상태를 저장하는 분산 key-value 저장소 |
| Kubelet API | 노드의 kubelet이 노출하는 API(10250) |
| kube-scheduler | Pod를 노드에 배치하는 컴포넌트(10251) |
| kube-controller-manager | 클러스터 컨트롤러 관리 컴포넌트(10252) |
| NodePort | 30000-32767 포트로 서비스를 외부에 노출하는 방식 |
| Kyverno | Kubernetes 정책 관리 엔진(PSS, 네트워크 격리 정책) |
| HA Control Plane | Control Plane 다중화(2개 이상) 구성, LB 필요 |
